#!/usr/bin/python3
"""
This Function validate the kind of class of an obj


def is_kind_of_class(obj, a_class):
     """Confirm if the object is exactly 
     an instance of the specified class"""
    

      if isinstance(obj, a_class):
        return True
    else:
        return False


